{
  "name": "octodo-rules-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Emulator tests for firestore-2.0.rules. See README.md.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project fantasktic-octodo \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
